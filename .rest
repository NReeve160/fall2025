### Base URLs
@local = http://localhost:8080
@render = https://fall2025.onrender.com

###
# Paste your JWT below after fetching it (or use the Google OAuth flow)
@token = REPLACE_WITH_JWT

### HEALTH
GET {{local}}/healthz

### AUTH - Who am I?
GET {{local}}/auth/me
Authorization: Bearer {{token}}

### CAMPAIGNS - List (PUBLIC)
GET {{render}}/campaigns/

### CAMPAIGNS - Create (PROTECTED)
POST {{render}}/campaigns/
Authorization: Bearer {{token}}
Content-Type: application/json

{
  "name": "Lost Mines Wednesday",
  "dm": "dm@example.com",
  "description": "Bi-weekly session",
  "players": ["p1@example.com"],
  "system": "D&D 5e",
  "status": "active"
}

### CAMPAIGNS - Get by id (PUBLIC)
# Replace {{cid}} with an id returned from Create
@cid = REPLACE_CAMPAIGN_ID
GET {{render}}/campaigns/{{cid}}

### CAMPAIGNS - Update (PROTECTED)
PUT {{render}}/campaigns/{{cid}}
Authorization: Bearer {{token}}
Content-Type: application/json

{
  "description": "Weekly session (updated)"
}

### CAMPAIGNS - Delete (PROTECTED)
DELETE {{render}}/campaigns/{{cid}}
Authorization: Bearer {{token}}

### ADVENTURERS - List (PROTECTED)
GET {{render}}/adventurers/
Authorization: Bearer {{token}}

### ADVENTURERS - Create (PROTECTED)
POST {{render}}/adventurers/
Authorization: Bearer {{token}}
Content-Type: application/json

{
  "name": "Arannis",
  "race": "Elf",
  "class": "Ranger",
  "level": 3,
  "alignment": "CG",
  "background": "Outlander",
  "hitPoints": 24,
  "campaign": null
}

### ADVENTURERS - Get by id (PROTECTED)
@aid = REPLACE_ADVENTURER_ID
GET {{render}}/adventurers/{{aid}}
Authorization: Bearer {{token}}

### ADVENTURERS - Update (PROTECTED)
PUT {{render}}/adventurers/{{aid}}
Authorization: Bearer {{token}}
Content-Type: application/json

{
  "level": 4,
  "hitPoints": 28
}

### ADVENTURERS - Delete (PROTECTED)
DELETE {{render}}/adventurers/{{aid}}
Authorization: Bearer {{token}}

### CAMPAIGN → ADVENTURERS (PROTECTED)
GET {{render}}/campaigns/{{cid}}/adventurers
Authorization: Bearer {{token}}
