#########################################
# Adventurers Guild API — LOCAL WORKFLOW
#########################################
@localUrl = http://localhost:8080
@localToken = PASTE_LOCAL_JWT_AFTER_LOGIN
@localCampaignId = REPLACE_AFTER_LIST
@localAdvId = REPLACE_AFTER_LIST

### 0) Local Dev Login (get JWT)
GET {{localUrl}}/auth/dev-login

### 1) Create Campaign (LOCAL)
POST {{localUrl}}/campaigns
Authorization: Bearer {{localToken}}
Content-Type: application/json

{
  "name": "Shattered Isles",
  "description": "Floating islands campaign.",
  "dm": "000000000000000000000010",
  "players": [],
  "system": "D&D 5e",
  "status": "active"
}

### 2) List Campaigns (LOCAL) — copy one _id into @localCampaignId
GET {{localUrl}}/campaigns
Authorization: Bearer {{localToken}}

### 3) Create Adventurer (LOCAL)
POST {{localUrl}}/adventurers
Authorization: Bearer {{localToken}}
Content-Type: application/json

{
  "name": "Dai Jing",
  "race": "Halfling",
  "class": "Monk",
  "level": 5,
  "alignment": "NG",
  "background": "Country doctor",
  "hitPoints": 38,
  "strength": 10,
  "dexterity": 18,
  "constitution": 12,
  "intelligence": 14,
  "wisdom": 16,
  "charisma": 12,
  "owner": "000000000000000000000001",
  "campaign": null
}

### 4) List Adventurers (LOCAL) — copy one _id into @localAdvId
GET {{localUrl}}/adventurers
Authorization: Bearer {{localToken}}

### 5) Link Adventurer to Campaign (LOCAL)
PUT {{localUrl}}/adventurers/{{localAdvId}}
Authorization: Bearer {{localToken}}
Content-Type: application/json

{
  "campaign": "{{localCampaignId}}"
}

### 6) List Adventurers in Campaign (LOCAL)
GET {{localUrl}}/adventurers?campaign={{localCampaignId}}
Authorization: Bearer {{localToken}}

### 7) Delete Adventurer (LOCAL)
DELETE {{localUrl}}/adventurers/{{localAdvId}}
Authorization: Bearer {{localToken}}

### 8) Delete Campaign (LOCAL)
DELETE {{localUrl}}/campaigns/{{localCampaignId}}
Authorization: Bearer {{localToken}}



#########################################
# Adventurers Guild API — RENDER WORKFLOW
#########################################
@renderUrl = https://fall2025.onrender.com
@renderToken = PASTE_RENDER_JWT_AFTER_LOGIN
@renderCampaignId = REPLACE_AFTER_LIST
@renderAdvId = REPLACE_AFTER_LIST

### 0) Render Dev Login (get JWT)
GET {{renderUrl}}/auth/dev-login

### 1) Create Campaign (RENDER)
POST {{renderUrl}}/campaigns
Authorization: Bearer {{renderToken}}
Content-Type: application/json

{
  "name": "Shattered Isles",
  "description": "Floating islands campaign.",
  "dm": "000000000000000000000010",
  "players": [],
  "system": "D&D 5e",
  "status": "active"
}

### 2) List Campaigns (RENDER) — copy one _id into @renderCampaignId
GET {{renderUrl}}/campaigns
Authorization: Bearer {{renderToken}}

### 3) Create Adventurer (RENDER)
POST {{renderUrl}}/adventurers
Authorization: Bearer {{renderToken}}
Content-Type: application/json

{
  "name": "Dai Jing",
  "race": "Halfling",
  "class": "Monk",
  "level": 5,
  "alignment": "NG",
  "background": "Country doctor",
  "hitPoints": 38,
  "strength": 10,
  "dexterity": 18,
  "constitution": 12,
  "intelligence": 14,
  "wisdom": 16,
  "charisma": 12,
  "owner": "000000000000000000000001",
  "campaign": null
}

### 4) List Adventurers (RENDER) — copy one _id into @renderAdvId
GET {{renderUrl}}/adventurers
Authorization: Bearer {{renderToken}}

### 5) Link Adventurer to Campaign (RENDER)
PUT {{renderUrl}}/adventurers/{{renderAdvId}}
Authorization: Bearer {{renderToken}}
Content-Type: application/json

{
  "campaign": "{{renderCampaignId}}"
}

### 6) List Adventurers in Campaign (RENDER)
GET {{renderUrl}}/adventurers?campaign={{renderCampaignId}}
Authorization: Bearer {{renderToken}}

### 7) Delete Adventurer (RENDER)
DELETE {{renderUrl}}/adventurers/{{renderAdvId}}
Authorization: Bearer {{renderToken}}

### 8) Delete Campaign (RENDER)
DELETE {{renderUrl}}/campaigns/{{renderCampaignId}}
Authorization: Bearer {{renderToken}}
